Square, rounded, icon-only button for toolbars and compact actions — always provide an accessible `label`.

```jsx
<IconButton icon={<i data-lucide="settings" />} label="Configurações" variant="soft" />
```

Variants: `ghost` (default), `soft` (green tint), `solid` (green fill). Sizes: `sm` / `md` / `lg`.
