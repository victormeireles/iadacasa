Circular avatar showing an image or initials derived from `name`.

```jsx
<Avatar name="Bar do Zé" color="terracotta" size="lg" />
```

Props: `name`, `src`, `size` (sm/md/lg), `color` (brand/accent/wine/terracotta).
