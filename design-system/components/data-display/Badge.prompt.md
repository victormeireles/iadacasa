Small pill label for status, counts and tags.

```jsx
<Badge variant="success" dot>Pronto</Badge>
<Badge variant="accent" tone="solid">Novo</Badge>
<Badge variant="brand">3 módulos</Badge>
```

Props: `variant` (neutral/brand/accent/success/warning/danger/info), `tone` (soft/solid/outline), `dot`, `icon`.
