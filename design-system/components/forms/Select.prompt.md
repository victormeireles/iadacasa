Styled native select with custom chevron.

```jsx
<Select label="Tipo de operação" placeholder="Selecione"
        options={['Hamburgueria', 'Pizzaria', 'Bar', 'Restaurante']} />
```

Pass `options` as strings or `{value,label}`, or `<option>` children.
